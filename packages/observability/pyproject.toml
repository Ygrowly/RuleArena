[project]
name = "rulearena-observability"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["pydantic-settings>=2.10.1,<3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rulearena_observability"]


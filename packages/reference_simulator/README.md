# Reference simulator

Pure, immutable reference model for the three commerce scenarios. It provides stable state
hashing, explicit transition outcomes, deterministic legal actions, seeded random search,
and bounded breadth-first search. This package does not access the network, a database,
Redis, or Commerce Sandbox implementation modules.

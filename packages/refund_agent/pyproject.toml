[project]
name = "rulearena-refund-agent"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "httpx>=0.28.1,<0.29",
  "rulearena-domain-contracts",
  "rulearena-reference-simulator",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rulearena_refund_agent"]

[tool.uv.sources]
rulearena-domain-contracts = { workspace = true }
rulearena-reference-simulator = { workspace = true }
